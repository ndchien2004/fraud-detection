<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.frauddetection</groupId>
        <artifactId>fraud-detection-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>load-test</artifactId>
    <name>load-test</name>
    <description>Gatling load test of POST /check-transaction (not part of the normal build)</description>

    <properties>
        <gatling.version>3.15.1</gatling.version>
        <gatling-maven-plugin.version>4.21.12</gatling-maven-plugin.version>
        <!-- the Spring Boot parent pins Netty 4.1; Gatling 3.15 needs 4.2 (NoClassDefFoundError: IoHandle).
             Overridden for this module only: the services keep Spring Boot's version. -->
        <netty.version>4.2.14.Final</netty.version>
    </properties>

    <dependencies>
        <!-- CardProfiles: each background card pays in its own home city, like the simulator -->
        <dependency>
            <groupId>com.frauddetection</groupId>
            <artifactId>common</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.gatling.highcharts</groupId>
            <artifactId>gatling-charts-highcharts</artifactId>
            <version>${gatling.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.gatling</groupId>
                <artifactId>gatling-maven-plugin</artifactId>
                <version>${gatling-maven-plugin.version}</version>
                <configuration>
                    <simulationClass>com.frauddetection.loadtest.CheckTransactionSimulation</simulationClass>
                    <!-- a slow system is a result to report, not a build failure -->
                    <failOnError>false</failOnError>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
